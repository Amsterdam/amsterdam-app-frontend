export enum CacheTimeout {
  none = 0,
  second = 1,
  minute = 60,
  fiveMinutes = 60 * 5,
  hour = 60 * 60,
  day = 60 * 60 * 24,
  week = 60 * 60 * 24 * 7,
}

export enum Preposition {
  from = 'vanaf',
  until = 'tot',
}
