import {ParkingSessionLicensePlateBottomSheet} from './ParkingSessionLicensePlateBottomSheet'
import type {Meta, StoryObj} from '@storybook/react'

const meta = {
  component: ParkingSessionLicensePlateBottomSheet,
} satisfies Meta<typeof ParkingSessionLicensePlateBottomSheet>

export default meta

type Story = StoryObj<typeof meta>

export const Default: Story = {}
