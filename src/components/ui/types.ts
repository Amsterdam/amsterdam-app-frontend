export enum Direction {
  up = 'up',
  down = 'down',
  left = 'left',
  right = 'right',
}

export type IconSize = 12 | 16 | 20 | 24 | 32

export enum Placement {
  above = 'above',
  below = 'below',
  before = 'before',
  after = 'after',
}
