import {BurningGuideNoAddressSection} from './BurningGuideNoAddressSection'
import type {Meta, StoryObj} from '@storybook/react-native-web-vite'

const meta = {
  component: BurningGuideNoAddressSection,
} satisfies Meta<typeof BurningGuideNoAddressSection>

export default meta

type Story = StoryObj<typeof meta>

export const Default: Story = {}
