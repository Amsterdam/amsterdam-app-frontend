export enum Direction {
  up = 'up',
  down = 'down',
  left = 'left',
  right = 'right',
}

export enum IconSize {
  sm = 12,
  md = 16,
  ml = 20,
  lg = 24,
  xl = 32,
}

export enum Orientation {
  landscape = 'landscape',
  portrait = 'portrait',
}

export enum Placement {
  above = 'above',
  below = 'below',
  before = 'before',
  after = 'after',
}
