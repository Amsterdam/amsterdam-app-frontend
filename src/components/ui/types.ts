export enum Direction {
  up = 'up',
  down = 'down',
  back = 'back',
  forward = 'forward',
}

export type IconSize = 16 | 20 | 24 | 32

export enum Placement {
  top = 'top',
  bottom = 'bottom',
  start = 'start',
  end = 'end',
}
