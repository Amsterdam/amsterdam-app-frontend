import {ParkingSessionBottomSheet} from './ParkingSessionBottomSheet'
import type {Meta, StoryObj} from '@storybook/react'

const meta = {
  component: ParkingSessionBottomSheet,
} satisfies Meta<typeof ParkingSessionBottomSheet>

export default meta

type Story = StoryObj<typeof meta>

export const Default: Story = {}
