import {ModuleSlug} from '@/modules/slugs'
import {ModuleClientConfig} from '@/modules/types'

export const aboutModule: ModuleClientConfig = {
  alwaysEnabled: true,
  name: 'AboutModule',
  slug: ModuleSlug.about,
}
